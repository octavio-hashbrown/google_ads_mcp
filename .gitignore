# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# VS Code
.vscode/settings.json
.vscode/launch.json

# Google Ads
google-ads.yaml

# temp file
.api-version
.mcp-server-version

# Env files
.env

# Test files
.coverage
.pytest_cache

# Written by ads_mcp/scripts/deploy_runtime.py into a deployment checkout.
# It records the exact commit that deployment was cut from, so it must never
# be committed back into the repository.
RUNTIME_REVISION
